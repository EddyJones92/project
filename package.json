{
  "name": "N-code_JS",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "update": "\"webdriver-manager update",
    "start": "webdriver-manager start",
    "testRun": "mocha --no-timeouts",
    "test": "start-server-and-test start 4444 testRun",
    "selenium": "mocha test/**/*.js --no-timeouts --reporter mochawesome --reporter-options reportDir=tests_lib\\test_reports,reportFilename=report,autoOpen=true"
  },
  "dependencies": {
    "chai": "^4.3.7",
    "chromedriver": "^109.0.0",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.7.1"
  }
}
